{
  "name": "storage-emitter",
  "description": "Emit events between browser tabs",
  "repository": "git://github.com/alekseykulikov/storage-emitter.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "keywords": [
    "storage",
    "onstorage",
    "emitter",
    "localstorage",
    "eventemitter"
  ],
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "jshint lib/ test/ && browserify-test",
    "build": "browserify -s sEmitter ./lib/index.js -o ./dist/storage-emitter.js",
    "development": "browserify-test --watch"
  },
  "dependencies": {
    "component-emitter": "^1.2.0"
  },
  "devDependencies": {
    "browserify": "^11.0.0",
    "browserify-test": "^1.3.0",
    "chai": "^3.1.0",
    "jshint": "^2.8.0"
  }
}
